from setuptools import setup

setup(
    name='lektor-${theme_id}',
    version='0.1',
    author=${author_name|pprint},
    author_email='${author_email}',
    license='MIT',
    py_modules=['${theme_module}'],
    entry_points={
        'lektor.themes': [
            '${theme_id} = ${theme_module}:${theme_class}',
        ]
    }
)
